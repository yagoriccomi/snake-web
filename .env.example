# Banco: o MESMO Supabase local do app Android DEV (scripts\db-dev start, no
# repositório snake-thai). Estes valores são lidos pelo navegador.
NEXT_PUBLIC_SUPABASE_URL=http://localhost:55321
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Backend próprio (snake-server). Local: o contêiner de desenvolvimento dele.
# Produção: a URL da Render.
NEXT_PUBLIC_API_URL=http://localhost:3000
